# File format magic numbers

The leading bytes (magic number / file signature) that identify a file format, with its usual extension. Answers 'what actually is this file' when the extension is wrong or missing.

**229 records.** Canonical, always-current version: [https://referencesource.org/file-format-signatures/](https://referencesource.org/file-format-signatures/)

| | |
|---|---|
| Last verified | 2026-09-02 |
| Re-check due | 2027-08-03 |
| Records | 229 |
| Machine-readable | [`data.json`](data.json) · [changes feed](https://referencesource.org/file-format-signatures/changes.xml) |

Every record carries `source` (the page it came from) and `source_quote` (the exact line on that page which states it), so any value here can be checked without asking us. Where a source does not state something the row is omitted rather than guessed.

**Licence position for this dataset.** CC BY-SA 4.0 (source: Wikipedia)

---

Snapshot of [referencesource.org](https://referencesource.org/file-format-signatures/), which is canonical and re-verified on a schedule. If a record here is wrong, that is worth more to us than one that is right — please open an issue.
